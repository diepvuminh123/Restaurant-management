{
  "name": "restaurant-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "connect-pg-simple": "^10.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^0.0.0",
    "recharts": "^3.8.1"
  },
  "scripts": {
    "dev": "concurrently -n BE,FE -c auto \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:be": "npm --prefix backend run dev",
    "dev:fe": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
